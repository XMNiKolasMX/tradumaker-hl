name: Anunciar Proyecto Finalizado

on:
  schedule:
    - cron: '0 12 * * 0'
  workflow_dispatch:

jobs:
  LlamarSupabaseFinalizados:
    runs-on: ubuntu-latest
    steps:
      - name: Ejecutar funcion de finalizados en Supabase
        run: |
          curl -X POST "https://vdwixgqicpdkyhslmqou.supabase.co/rest/v1/rpc/anunciar_proyecto_finalizado" \
          -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \
          -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
          -H "Content-Type: application/json"
